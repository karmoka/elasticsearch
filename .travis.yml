language: java
install: true


dist: trusty
jdk:
  - openjdk13

matrix:
  allow_failures:
    - jdk: openjdk-ea

branches:
  only:
  - master
 
script:
  - echo $JAVA_HOME
  - java -version
  - ./gradlew --continue clean build --scan -s -p modules/percolator